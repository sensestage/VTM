VTMContextManagerListView : VTMContextView {
	*new{arg parent, bounds, context, description, definition;
		^super.new(parent, bounds, context, description, definition).initContextManagerListView;
	}

	initContextManagerListView{

	}
}
